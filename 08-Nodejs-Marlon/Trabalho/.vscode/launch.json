{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Node: Nodemon",
            "processId": "${command:PickProcess}",
            "restart": true,
            "protocol": "inspector",
            "env": {
                "PORT": "3000",
                "MONGODB_URI": "mongodb://localhost:27017/trabalho"
            }
        }
    ]
}