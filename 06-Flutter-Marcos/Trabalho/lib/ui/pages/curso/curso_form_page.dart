import 'package:flutter/material.dart';

class CursoFormPage extends StatelessWidget {
  const CursoFormPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
